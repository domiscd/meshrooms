This directory contains test fixtures for WormDB persistence adapter tests.

- `corrupted-wal/`: Contains an intentionally corrupted WAL file to test how the engine behaves during recovery from truncated or invalid records.
