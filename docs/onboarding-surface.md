# First-run setup and local settings

Mode: Operate. Target: `src/FirstRun.tsx`.

The approved creator flow gets a human from an agent-opened local link into the requested room with a distinct agent participant. Preserve selected layout A: navy rail, white working surface, Manrope, existing action colors and restrained borders. This is a focused setup form, with no tour, variants, marketing content or new visual world.

First run has two compact steps: human/machine labels, then startup preferences and explicit room review. The initiating agent is named separately. Advanced exposes the real storage path as read-only; migration and MeshGuard networking remain unavailable. Startup preference and actual installed startup entry are described separately.

Returning setup requests show only the requested room/agent review, with explicit creation. Completed intents open the existing room. Settings are separate and keep the current room mounted so unpublished room work survives. No room is created by loading a URL or GET request.

Access tickets are removed from the fragment immediately and exchanged for an owner cookie. Unauthorized views show the agent entry/retry state without a room or demo fallback. Explicit `?demo=1` bypasses onboarding. Agent presence comes from backend connection evidence, not registration alone.

Verification: one desktop/mobile round for setup, returning review, settings and unauthorized states; at most one batched fix/confirmation. Check explicit action, unchanged retry IDs on uncertain failure, interrupted/repeated setup, scoped room drafts, startup truth and agent presence. Existing design sidecar descriptions of retired variants remain stale and are outside this change.
